services:
  rust-kv:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rust-kv
    ports:
      - "3000:3000"
    environment:
      - RUST_LOG=info
    restart: unless-stopped