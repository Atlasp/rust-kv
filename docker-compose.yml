services:
  rusty-audio-bridge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rusty-audio-bridge
    ports:
      - "3000:3000"
    environment:
      - RUST_LOG=info
    restart: unless-stopped
    # Uncomment if you need to mount volumes
    # volumes:
    #   - ./data:/app/data